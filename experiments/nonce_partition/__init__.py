"""Disjoint-nonce worst-case experiment (branch: pocol-disjoint-nonce-worst-case).

A SEPARATE, explicitly labelled experiment. It does NOT modify the corrected
continuous-mining comparison (results/corrected/) or the wall-clock energy model
(Models/Node.py). See docs/NONCE_PARTITION_WORST_CASE_METHODOLOGY.md and
docs/NONCE_PARTITION_SCOPE_AND_LIMITATIONS.md.
"""
