# Stage 5B1A logs

Per-run stdout/stderr logs for the bounded validation are written alongside their
probe artifacts under `../storage_probe/*.stdout.log`. Stage 5B2 will write its
per-run execution logs into this directory.
