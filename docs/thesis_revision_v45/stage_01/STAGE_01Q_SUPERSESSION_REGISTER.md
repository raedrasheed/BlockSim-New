# Stage 1Q — Supersession Register

Stage 1Q supersedes specific Stage-1P statements by RECORDING the supersession here and updating ONLY the
un-suffixed normative documents (`STAGE_01_*.md` / `STAGE_01_*.csv`). Historical Stage-1A…1P lettered
artifacts (`STAGE_01A_*`…`STAGE_01P_*`) are NOT modified — a superseded statement may still appear verbatim in
a frozen lettered artifact as a historical record. Name remains **PoCol**; mechanism is **the idle policy
within PoCol**; the A1 baseline (`8.420833333 kWh`) is unchanged.

| # | Superseded statement (stage · location) | Superseding statement (Stage 1Q · location) | Rationale |
|--:|------------------------------------------|----------------------------------------------|-----------|
| S1 | **P5** — recovery-decision freshness is judged by whether a NEWER `RecoveryDecisionID` exists (`latest_recovery_decision`) (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §9/§10a; `STAGE_01P_*`) | **Q1** — every FINAL recovery census is VERSIONED (`recovery_census_seq`, `latest_recovery_census[episode]`); a decision binds to a `RecoveryCensusVersion` and `ReconcilePendingRecoveryDecisions` supersedes a contradicted decision (even with no new completion) or re-affirms a consistent one (pseudocode §0.8/§9) | Freshness must be judged against EVERY final event-time census, not merely the existence of a newer decision id |
| S2 | **N2/P5** — `CompleteSecurityRecovery` is a QUEUED event that APPLIES the outcome (transition/abort) on dispatch (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §10a; `STAGE_01P_*`) | **Q2** — a TWO-STEP contract: `RecoveryCompletionDueEvent` (queued; records due + refreshes census, NO transition) then `ApplyRecoveryCompletionAfterEpilogue` (post-epilogue hook) applies ONLY after the event_time's final census version is published, and only if fresh + matching; `CompleteSecurityRecovery` becomes an internal branch dispatch (pseudocode §0.7d/§10a) | An outcome must not be applied before the FINAL census of its own application `event_time` is known; a RESTORED decision must not leave recovery under a newer breach census |
| S3 | **P4** — a single boolean `recovery_completion_pending[episode]` tracks the pending completion (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §0.8/§9; `STAGE_01P_*`) | **Q3** — explicit decision identities: `RECOVERY_DECISION_STATUS in {CREATED, SCHEDULED, SUPERSEDED, APPLIED, SCHEDULE_FAILED, CANCELLED}`, `recovery_decisions` (per-decision record), and `pending_recovery_decisions[episode]` (a SET); supersede + cancel BEFORE seating a replacement; a failed superseding schedule leaves the old decision SUPERSEDED (pseudocode §0.8/§9/§10a) | Multiple superseded/scheduled completions may coexist; one boolean cannot represent them, and a failed replacement must never revive the earlier decision |
| S4 | **H3/K7/P3** — the security-census maps have "two coherent writers"/"a third writer"/"`ApplyMinerStateTransition` is the SOLE writer" (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §0.8/§0.9/§9/§9a; `STAGE_01H_*`…`STAGE_01P_*`) | **Q4** — ONE canonical atomic writer `CommitSecurityCensus` (§0.8a) with THREE `census_source` values {MINER_STATE_TRANSITION, APPLICABILITY_ENTRY, RECOVERY_DEADLINE}; the three sources CALL it; `dirty[t] ⇒ latest[t]` is structural (pseudocode §0.8a/§0.9/§9/§9a; catalogue I17) | The contradictory writer-count statements are removed; a single atomic writer makes the coherence invariant structural |
| S5 | **I-04/K8/P2** — per-run state (EventQueueContext, RunHookContext, security-census maps, rebased_boundaries, run_finalised) is created INLINE in `RoundInitialise`'s `IF prior_state = null` block; `RunHookContext` reached as an implicitly-created object (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §1; `STAGE_01P_*`) | **Q5** — an explicit `RunContext` created ONCE by `RunInitialise` (§1.0); `RoundInitialise` receives it, inits only per-round registries, preserves it, and RETURNS every per-round recovery registry explicitly; `RunEventLoopToHorizon` obtains `RunHookContext` through `RunContext.RunHookContext` (pseudocode §1.0/§1.1/§0.7d-run) | Run-level and round-level ownership must be explicit and complete; no registry may be an implicit global |
| S6 | **P2** — the run-hook envelope's collision freedom rests on the RESERVED numeric `RUN_HOOK_CYCLE` delta-cycle; `applied_run_hook_ids` is a set of applied ids (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §0.7e/§20b; `STAGE_01P_*`) | **Q6** — a tagged `envelope_namespace in {ORDINARY_EVENT, RUN_HOOK}` (§0.2); collision freedom derives from the TAG (+`hook_id`), not the number; `TransitionEventID` includes `envelope_namespace`/`hook_id`; `applied_run_hook_ids` carries an IN_PROGRESS/APPLIED state so a partial close cannot replay as a full close (pseudocode §0.2/§0.7e/§20b) | Collision freedom must not rest on an unexplained magic number; a partial run-hook invocation must not replay as a second full close |
| S7 | **N2/P5** — the recovery completion is scheduled at an undefined `t_next` (strictly > t) (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §9; `STAGE_01P_*`) | **Q7** — a DETERMINISTIC `target_time = decision_time + configured_recovery_completion_delay` (config `> 0`, or the next-representable instant); a target `> T` is not enqueued and records `horizon_deferred` (pseudocode §0.8/§9) | The recovery completion time must be deterministic and fully defined, not an undefined `t_next` |

## Notes

- Every supersession above updates only un-suffixed normative documents. The frozen lettered artifacts
  `STAGE_01A_*`…`STAGE_01P_*` are byte-unchanged by Stage 1Q (verified by `git diff --name-only <base>`);
  their historical statements are preserved deliberately.
- No supersession changes the A1 baseline (`8.420833333 kWh`), introduces a new consensus feature, or claims
  any energy/security/fairness property. The prohibited rebranded-algorithm-name variants are not used
  anywhere in this document.
