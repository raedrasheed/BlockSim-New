# Stage 1O — Supersession Register

Stage 1O supersedes specific prior-stage statements by RECORDING the supersession here and updating ONLY the
un-suffixed normative documents (`STAGE_01_*.md` / `STAGE_01_*.csv`). Historical Stage-1A…1N lettered
artifacts (`STAGE_01A_*`…`STAGE_01N_*`) are NOT modified — a superseded statement may still appear verbatim in
a frozen lettered artifact as a historical record. Name remains **PoCol**; mechanism is **the idle policy
within PoCol**; the A1 baseline (`8.420833333 kWh`) is unchanged.

| # | Superseded statement (stage · location) | Superseding statement (Stage 1O · location) | Rationale |
|--:|------------------------------------------|----------------------------------------------|-----------|
| S1 | **N1** — `FinalizeSimulationRun` is "a RUN-LEVEL terminal event seated on the queue at `(T, RUN_FINALISE)`" and it "drains the event queue to quiescence for every `event_time <= T`" and "horizon-closes a nonterminal round" (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §20a; `STAGE_01N_*`) | **O1** — `FinalizeSimulationRun` is a NON-queued, post-`ProcessEventTime(T)` RUN-LEVEL hook that performs ONLY the single `FINAL_RUN_END` `SettleResidencyBoundary` + I5/I6/I7 + `run_finalised`; the internal DRAIN is REMOVED (the run driver `RunEventLoopToHorizon`/`ProcessEventTime` drains) and the horizon-close is `CloseRoundAtHorizon` (§20b) (pseudocode §0.7d-run/§20a/§20b) | ProcessEventTime must be the sole event-loop driver, never re-entered from a handler; a finaliser that re-drains the queue is a re-entrant driver |
| S2 | **N3** — `FinalizeSimulationRun` has a §0.7g-driver seating row and a `RUN_FINALISE` microphase "processed after every ordinary round event" (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §0.7g-driver/§21; `STAGE_01N_*`) | **O1** — `RUN_FINALISE` is NOT in the ordinary microphase queue map; `FinalizeSimulationRun` and `CloseRoundAtHorizon` are run-level hooks removed from the seating table and documented as never enqueued (pseudocode §0.7g/§0.7g-driver/§21) | A run-level hook is not an enqueued microphase; the seating table lists only queued driver entry points |
| S3 | **J9/K8** — `ScheduleEvent` rejects only finalised `event_time`s and backward `event_time`s (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §0.7e; `STAGE_01J_*`/`STAGE_01K_*`) | **O2** — `ScheduleEvent` ALSO rejects `target_event_time > run_horizon_T` (`post_horizon_event_rejected`); `= T` legal; so no pending ordinary event ever has `event_time > T` (pseudocode §0.7e) | Bind scheduling to the horizon so nothing is stranded past `T` and no recovery action changes `H_active` after the final decision at `T` |
| S4 | **N2** — `CompleteSecurityRecovery` precondition `floor_result = restored` WITH a branch `IF NOT floor_result = restored: RoundAbort(...)` (contradiction; R14 unreachable) (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §10a; `STAGE_01N_*`) | **O3** — precondition `RecoveryOutcome in {RESTORED, UNRECOVERABLE}`; RESTORED → A/B/C, UNRECOVERABLE → D → `RoundAbort(floor_unrecoverable)`; the named seated source `RecoveryDeadlineEvent` (§9a) supplies UNRECOVERABLE, making R14 reachable (pseudocode §9/§9a/§10a) | R14 must have a concrete source and call path to be "executable"; a contradictory precondition/branch is dead code |
| S5 | **N2** — "ENSURE exactly one `CompleteSecurityRecovery` is scheduled for THIS recovery episode" (prose only) (`STAGE_01_PROTOCOL_PSEUDOCODE.md` §9; `STAGE_01N_*`) | **O4** — a per-episode registry keyed by `RecoveryEpisodeID`: `recovery_completion_pending` (at most one seated) and `recovery_outcome_finalised` (at most one applied); a duplicate/stale completion is a deterministic no-op (pseudocode §0.8/§9/§9a/§10a) | Idempotence must be a registry key, not a prose "ensure exactly one" that no procedure enforces |
| S6 | **H3** — the single settled-census evaluation is `FinalizeTimestampSecurityCensus`, running "in microphase 5 per settled `(event_time, delta_cycle)`" — placed BEFORE certificate/discovery in the same-timestamp order (`STAGE_01_ROUND_STATE_MACHINE.md` §2.6; `STAGE_01_TERMINOLOGY.md`; `STAGE_01_INVARIANT_CATALOGUE.md`; `STAGE_01_TRACEABILITY_MATRIX.csv` R51; `STAGE_01H_*`) | **O5** — the decision is `FinalizeEventTimeSecurityCensus`, the event-time EPILOGUE keyed by `event_time` alone (I-01/I-02), run AFTER the whole `event_time` is quiescent — NEVER before certificate/discovery; the superseded name is replaced everywhere in the normative corpus (round SM §2.6, terminology, catalogue I17, traceability R51) | The security decision was relocated to the post-quiescence epilogue by I-01/I-02; the stale name and the "before certificate/discovery" ordering contradicted the current model |
| S7 | **N1** — round SM §3.14 says `FinalizeSimulationRun` "drains all events permitted up to the final `event_time`" and "closes it through the declared horizon-end round disposition" (`STAGE_01_ROUND_STATE_MACHINE.md` §3.14; `STAGE_01N_*`) | **O1** — §3.14 states the canonical horizon sequence: the run driver drains via `ProcessEventTime`; `CloseRoundAtHorizon` (§20b) closes a nonterminal round; then `FinalizeEventTimeSecurityCensus(T)` (terminal-stale-noop); then the run-level `FinalizeSimulationRun` performs only the settle + reconciliation (round SM §3.14) | The round-SM narrative must match the O1 split of responsibilities |

## Notes

- Every supersession above updates only un-suffixed normative documents. The frozen lettered artifacts
  `STAGE_01A_*`…`STAGE_01N_*` are byte-unchanged by Stage 1O (verified by `git diff --name-only <base>`);
  their historical statements are preserved deliberately.
- No supersession changes the A1 baseline (`8.420833333 kWh`), introduces a new consensus feature, or claims
  any energy/security/fairness property. The prohibited rebranded-algorithm-name variants are not used
  anywhere in this document.
