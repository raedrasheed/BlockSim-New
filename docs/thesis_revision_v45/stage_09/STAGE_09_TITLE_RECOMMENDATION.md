# Stage 9 — Title Recommendation

The applied thesis title is **left unchanged** unless the user explicitly authorises a
change. This document records scientifically precise candidates and the existing-title
risk. No title edit is applied automatically in the redline.

## Current applied title (draft-44)

The document title block is retained as-is. Any perception that it promises PoW-level
throughput or unconditional energy superiority is mitigated in the abstract and results,
not by an unauthorised title change.

## Recommended candidates (in preference order)

1. **Proof-of-Collaboration: Design and Evaluation of an Energy-Aware Coordinated
   Proof-of-Work Consensus Protocol**
   — Accurate: PoCol is a coordinated variant of PoW (retains the hash puzzle and
   longest-chain rule); "energy-aware" claims awareness, not a proven universal saving;
   "evaluation" signals a simulation study, not a deployment result.

2. **Proof-of-Collaboration: A Coordinated Nonce-Domain Consensus Protocol for
   Energy-Efficient Blockchain Mining**
   — Accurate on the mechanism (coordinated nonce-domain allocation); "energy-efficient"
   is the design intent. Slightly stronger than candidate 1 because "energy-efficient"
   can be read as an established property; prefer candidate 1 if the examiners want the
   most conservative framing.

Both name the algorithm **PoCol / Proof-of-Collaboration** and introduce no new name.

## Existing-title risk

* Any title asserting "energy-efficient" as an achieved outcome should be read against
  the frozen evidence: energy reductions are **calculated low-power-state residency**
  reductions relative to a within-run power-null (Stage 8S, 55.05%) or a matched
  same-template PoW control (Stage 8U, 44.95%), under a reduced-scale simulator, with the
  joint operational acceptance rules **not** satisfied. Candidate 1 ("energy-aware …
  evaluation") is the safest match to that evidence.
* A title implying comparison with Bitcoin or real-world PoW would be unsupported and
  must be avoided.

## Action

Recommendation recorded only. The applied title is unchanged in the Stage-9 redline and
clean copies. If the user authorises a change, candidate 1 is the recommended
replacement and would be applied as a tracked (red) edit.
