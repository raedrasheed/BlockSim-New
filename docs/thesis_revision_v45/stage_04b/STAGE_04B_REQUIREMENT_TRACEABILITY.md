# Stage 4B — Requirement Traceability

Every Stage-4B requirement (S4B-1 … S4B-8) mapped to its implementation and its locking test(s).
All 12 Stage-4B tests plus the 103 retained tests pass (**115 total**). Baseline
`0ab8e075c8d75aeabd19918c51690752077ee312`; `search.py` byte-identical.

| Req | Requirement | Implementation (`Models/PoCol/stage2/`) | Locking test(s) |
|-----|-------------|------------------------------------------|-----------------|
| **S4B-1** | On EXPIRED / CANCELLED / REVOKED / PROGRESS_TIMEOUT terminalisation, cancel the exact HashWork / RangeExhaust / expiry / timeout events, mark the search state terminal, transition the miner OUT of ACTIVE_HASHING / WAKING immediately (FAILED→OFFLINE; CANCELLED / EXPIRED / TIMEOUT→LOW_POWER_LISTEN), cancel the queued WakeCompleteEvent, and stop `P_hash` / `P_wake`; a lease expiring before wake never activates the miner, and a terminalised miner is never re-recruited. | `simulator.py`: `_TRIGGER_TERMINALISATION` 3-tuples, `_LIVE_MINER_STATES`, `RevokeRangeLeaseTransaction` (miner transition + `miners_terminalised_with_lease`), `_cancel_queued_hash_events` (returns `wakes_cancelled_with_lease`), `_eligible_reassignment_candidates` (`completion_kind == "EXHAUSTED"` gate) | `test_s4b_01`, `test_s4b_02`, `test_s4b_03` |
| **S4B-2** | `EvaluateRangeLease` rejects a trigger not in `LEASE_TRIGGERS` → `range_lease_observation_rejected_unknown_trigger`, no mutation / counter / state change, no default REVOKED fallback; exact replay returns `range_lease_observation_already_exists(observation, decision_or_null, outcome_or_disposition)`, never `None`. | `simulator.py`: `EvaluateRangeLease` unknown-trigger guard (`unknown_trigger_rejections`) + replay branch reading the stored `(obs, rdid, outcome)` triple; `_finish` stores it in `lease_observation_by_key` | `test_s4b_04` |
| **S4B-3** | With leases enabled, a `RangeExhaustEvent` MUST carry its `LeaseID`; verify lease exists + ACTIVE + current, owns the exact miner + assignment (+version), generation and search-generation match, search state EXHAUSTED, cursor == frontier == `range_end`, round / template current; missing / premature / stale = no state / floor / termination effect. | `simulator.py`: `_verify_exhaust_lease` (strict, rejects missing `LeaseID`) guarding `_handle_range_exhaust`; `stale_exhaust_events` | `test_s4b_05`, `test_s4b_06` |
| **S4B-4** | Do NOT instantiate a `RangeSlice` for a wake handle; create a `ReassignmentWakeHandle{WakeHandleID, RoundID, TemplateID, OriginalRangeSliceID, RangeReassignmentRequestID, MinerID, generation, status}`; activation references those with `activation_scope = REASSIGNMENT_WAKE_ONLY`; the wake handle is NOT in any nonce-domain registry; the original `RangeProgress` is the sole interval authority. | `leases.py`: `ReassignmentWakeHandle`; `simulator.py`: `_seat_reserve_reassignment_wake`, `_obj_activation_id`, `_audit_slice_disjointness`; `context.py`: `reassignment_wake_handles`, `wake_handles_created` | `test_s4b_08` |
| **S4B-5** | For a Path-B `ReserveActivationStartEvent` OR `CompleteEvent` seating failure, terminalise the linked activation + `RangeReassignmentRequest` FAILED, close the wake-energy interval, remove the wake handle + reverse links, preserve the committed frontier, `current_lease_id = None`, apply the retry / no-eligible policy; assert no WAKING miner, no SEATED / STARTED request, no live activation, no orphan, no horizon hang. | `simulator.py`: `_seat_reserve_reassignment_wake._rollback_wake` (start-seat point), `_fail_pathb_wake` + `_handle_reserve_activation_start` failure branch (complete-seat point), `_maybe_terminate_no_block`; `pathb_rollback_count`, `wake_complete_seat_failures` | `test_s4b_09`, `test_s4b_10` |
| **S4B-6** | Natural replay returns `range_reassignment_already_exists(request, status, start_event_ref, complete_event_ref, successor_lease, disposition)` with the exact stored values; no counter increment; no event / request / decision / lease creation. | `simulator.py`: `SeatRangeReassignmentTransaction` (`reassignment_by_predecessor` guard) → `_reassignment_already_exists` | `test_s4b_11` |
| **S4B-7** | Per-request energy (predecessor active-before / idle-after, reassignee standby-before-wake, wake, active-hashing) + all lifecycle timestamps + status / disposition, for COMPLETED / FAILED / CANCELLED; do NOT omit failed wake energy; per-request rows + aggregate; verify against residency deltas. | `leases.py`: `RangeReassignmentRequest` interval timestamps + residency snapshots; `simulator.py`: capture at revoke / wake-start / search-start / terminal; `adapter.py`: `reassignment_energy_report` (per-request rows incl. FAILED / CANCELLED + `aggregate` + `max_energy_residual_j`) | `test_s4b_12` |
| **S4B-8** | Every causal committed-frontier advance increments `progress_generation`, updates `last_progress_time`, increments `timeout_generation`, supersedes the old timeout, seats one new timeout; the expiry / timeout handlers verify all declared payload fields; stale / tampered deadline events have no effect. | `simulator.py`: `_handle_hash_work` advance block (`progress_generation += 1`, `last_progress_time`, `_rearm_lease_deadlines`), `_seat_lease_expiry` / `_refresh_progress_timeout` (fresh identity), `_verify_deadline_identity`, deadline re-arm after the `SOLUTION_PROPAGATION` transition in `_handle_prepare_participants` | `test_s4b_07` |

## Retained coverage (103 tests, unchanged)

* Stage-2 core + adapter + scientific + E2E + semantic vectors (TV325–TV338, E2E-1..5, SCI-1..10).
* Stage-3 / Stage-3A security-floor + reserve activation (S3-01..S3-18, S3A-01..S3A-12).
* Stage-4 / Stage-4A range leases + reassignment (S4-01..S4-20, S4A-01..S4A-14).

Backward compatibility is structural: `RangeLeasePolicy.enabled = False` by default. The single
white-box edit to `test_stage4a_lease_lifecycle.py` (S4A-10) renames the queried registry
`reassignment_wake_slice_by_id → reassignment_wake_handles` to follow the S4B-4 refactor; its
intent (no orphan Path-B token after a start-seat rollback) is preserved.
