# STAGE 05A — Final Stage-5 Matrix (FROZEN DESIGN — not executed)

Machine-readable: `STAGE_05A_FINAL_MATRIX.csv` (one row per planned run, all
config fields + `configuration_hash` + `expected_output_path` +
`planned_status`). Generated by `experiments/thesis_revision_v43/build_matrix.py`.
Seed schedule (frozen): `results/thesis_revision_v43/stage_05a/raw/seed_schedule.json`
(SHA-256 `6122dbd0…b13e10`). Hypothesis→run map:
`.../raw/hypothesis_to_run.json`. Summary: `.../raw/matrix_summary.json`.

## Totals

**Total planned runs: 1 890** (≤ 3 500 ceiling). **Duplicate configurations
removed: 0** (deduplicated by `configuration_hash`).

| matrix class | runs | purpose |
|---|---|---|
| CORE | 600 | B0, B1, B2, B3_C1 × {100..500} × 30 seeds (H1, H2) |
| CORE_C2 | 150 | C2 homogeneous μ=2 idle=0 × {100..500} × 30 (H3, H4) |
| SENS_IDLE | 300 | C2 μ=0.5 × {100,500} × idle{0,5,10,20,30}% × 30 (H3) |
| SENS_HETERO | 240 | {B3_C1, C2} × heterog × {equal,weighted} × {100,500} × 30 (H5) |
| SENS_MU | 120 | B3_C1 × μ{0.5,1} × {100,500} × 30 (H8) |
| SENS_INACTIVE | 180 | B3_C1 × inactive{5,15,30}% × {100,500} × 30 (H6) |
| SENS_DELAY | 240 | B3_C1 × delay{0,5,30,60}s × {100,500} × 30 (H7) |
| EXPLORATORY | 60 | B3_C1 × heterog-high × {100,500} × 30 (separate, not pooled) |

By scenario: B0/B1/B2 = 150 each; **B3_C1_CONTINUOUS_DISJOINT = 870** (one
dataset, labels "B3;C1"); C2 = 570.

## Baseline (held constant unless the row varies it)
`H_total = 141 TH/s`, `21.5 J/TH`, duration `10 000 s`, interval `600 s`,
delay `0.42 s`, homogeneous, `μ = 2`, inactive `0 %`, idle `0 %`,
`disjoint_equal` allocation, exact Binomial sampler.

## Redundancy eliminations (proven, documented)
- **C2 homogeneous μ=2 idle-ratio sweep is NOT executed in core:** proven in
  Stage 4 and confirmed in the pilot (idle_time = 0 → idle power irrelevant → all
  ratios identical). The idle-ratio effect is instead measured under μ=0.5
  (SENS_IDLE), where ranges complete early. This removes ≈ 600 redundant runs.
- **Sensitivity levels exclude the base value** (e.g. delay excludes 0.42, μ
  excludes 2, inactive excludes 0) — the base point already exists in CORE.

## Pseudoreplication controls
- **B3 and C1 → one underlying run** (`interpretation_labels="B3;C1"`,
  `underlying_execution_model="pocol_continuous"`). Stage 6 counts it once.
- Reused runs carry one `run_id`; the hypothesis map lists run_ids per hypothesis.

## Heterogeneous distribution (frozen proposal)
`HETEROGENEOUS_MODERATE` = log-normal, σ = 0.5, normalized to `H_total`
(diagnostics in `matrix_summary.json`): Gini ≈ 0.31 (N=100) / 0.28 (N=500),
max share ≈ 4–5× mean. `HETEROGENEOUS_HIGH` (exploratory) = σ = 1.0, Gini ≈
0.54–0.57, max share ≈ 12–19× mean. Configured simulator hash rate is **not**
claimed to be externally verifiable in a permissionless deployment.

## Execution model per scenario (for Stage 5B)
| scenario | model | note |
|---|---|---|
| B0 | Bitcoin full sim (model 1) | independent-template PoW abstraction |
| B3_C1 | PoCol full sim (model 3) | continuous disjoint |
| C2 | PoCol full sim + idle transform | idle energy from state durations |
| B1, B2 | direct coverage model | **full event-loop implementation is a Stage-5B prerequisite** (see pilot report §limitations) |

## Estimated cost (Stage 5B)
≈ 1 890 runs × ≈ 1.5–3 s ≈ **1.5 CPU-hours** (~10 min at ~14 workers); storage
= summary row + diag per run ≈ **10 MB** + full logs for the retained subset.
